syntax = "proto3";

package ml.v1;


option go_package = "github.com/bimal009/go-core/gen/ml;mlv1";

service ChatService {
  rpc Send(ChatRequest) returns (ChatResponse);
}

message ChatRequest {
  string message = 1;
}

message ChatResponse {
  string reply = 1;
}